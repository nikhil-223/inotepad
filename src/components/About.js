import React, {useContext, useEffect} from 'react'
import noteContext from '../context/note/noteContext'

const About = () => {
  // const a= useContext(noteContext)
  
  return (
    <div>my name is  and i am in class </div>
  )
}

export default About